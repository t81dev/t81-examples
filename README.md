# t81-examples
Curated demo

## Runtime v0.4 E2E Bundle

See `examples/runtime-v0.4-e2e/README.md` for a deterministic end-to-end path:

1. emit canary bytecode via `t81-lang`,
2. execute in `t81-vm`,
3. execute again through the `t81-python` VM bridge.
